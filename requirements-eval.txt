# Lower bounds only, not exact pins. These reflect versions confirmed to
# work together during development, not necessarily the exact environment
# used for the original arXiv v1 (Aug 2025) experiments — this project's
# eval steps historically ran across several separate, ad hoc environments
# with drifting shared-dependency versions (e.g. numpy 1.26–2.4, torch
# 2.3–2.11 across different steps), so an exact single-environment pin
# would overstate the precision of this reproducibility claim. See
# scripts/eval/README.md.
Levenshtein>=0.27.1
jaconv>=0.4.0
pandas>=2.3.1
modelscope>=1.20.0
soundfile>=0.12.1
scipy>=1.12.0
torch>=2.3.1
utmosv2>=1.2.0
tqdm>=4.67.1
numpy>=1.26.4
statsmodels>=0.14.5
